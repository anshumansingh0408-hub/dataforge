services:
  - type: web
    name: quickorder
    env: node
    buildCommand: npm install --no-package-lock
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: RIME_API_KEY
        sync: false
